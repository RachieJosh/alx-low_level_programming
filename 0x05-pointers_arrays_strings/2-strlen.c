#include "main.h"
#include <stdio.h>
#include <string.h>

/**
  * _strlen - Returns the length of a string
  * @s: Character to be checked
  *
  * Return: The length of the string
 */

int _strlen(char *s)
{
	char *s;
	int len;

	len = strlen(*s);
}
